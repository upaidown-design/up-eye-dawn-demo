import{test,expect}from'@playwright/test';
test('internal round decision simulator is isolated and calculates mechanics',async({page})=>{await page.goto('/demo/dev/round-decision');await expect(page.getByRole('heading',{name:'ROUND DECISION — NEW YORK 2026'})).toBeVisible();await expect(page.getByText('AWAITING FOUNDER APPROVAL',{exact:true})).toBeVisible();await expect(page.getByText('€1M core',{exact:true})).toBeVisible();await expect(page.locator('header nav').getByText(/round decision/i)).toHaveCount(0);await expect(page.getByText('€7M',{exact:true})).toBeVisible();await expect(page.getByText('14.3%',{exact:true})).toBeVisible();await page.getByLabel('Investment').fill('1500000');await expect(page.getByText('€7.5M',{exact:true})).toBeVisible();await expect(page.getByRole('definition').filter({hasText:'20%'})).toBeVisible();await expect.poll(()=>page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth)).toBe(true)});
